server:
  port: 8902

spring:
  application:
    name: client-feign-zpf

eureka:
  client:
    #eureka服务地址
    service-url:
      defaultZone: http://localhost:8848/eureka
feign:
  hystrix:
    #开启断路器
    enabled: true