server:
  port: 8904

spring:
  application:
    name: client-feign-zly

eureka:
  client:
    #eureka服务地址
    service-url:
      defaultZone: http://localhost:8848/eureka
feign:
  hystrix:
    #开启断路器
    enabled: true